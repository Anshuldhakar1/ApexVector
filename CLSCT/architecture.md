This will be a python module "apx-clsct".

CLI parameters expected.
 -   [--input, -i]  { input file path }
 -   [--output, -o] { output file path / optional use inp name if not given }
 -   [--debug ]     { Uses matplotlib to output an intermediate snapshot of the pipeline at each checkpoint }
 -   [--mode ]      { for different pipelines / pipelines (future proofing) with will be injection (code injection pattern). This will only be for pipelines }



